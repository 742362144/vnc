[Kubernetes]
token_file = /root/.kube/config

[WatcherTimeout]
timeout = 31536000

[Node]
plural = virtualmachines
group = cloudplus.io
version = v1alpha3

[VirtualMachine]
plural = virtualmachines
group = cloudplus.io
version = v1alpha3
